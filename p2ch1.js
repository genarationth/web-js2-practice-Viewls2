let birthYear = 1999;
let futureYear = 2023;
currentAge = futureYear - birthYear;
possibleAge = currentAge - 1;
console.log(`If you were born in ${birthYear}, then in ${futureYear} you'll be ${possibleAge} or ${currentAge} (depending on the month in ${futureYear}).`);